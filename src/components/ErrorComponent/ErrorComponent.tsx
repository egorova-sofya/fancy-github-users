const ErrorComponent = () => {
  return (
    <p style={{ padding: "12px var(--padding-left-right)" }}>
      Something went wrong. Try reloading the page
    </p>
  );
};

export default ErrorComponent;
